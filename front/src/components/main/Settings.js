import React from 'react';

const Settings = () => {
    return (
        <div className="settings-container">
            <h2>설정</h2>
            {/* 설정 항목 */}
        </div>
    );
};

export default Settings;
